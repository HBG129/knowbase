[package]
name = "knowbase"
version = "0.1.0"
description = "Private AI knowledge workspace for local documents."
authors = ["HBG129"]
edition = "2021"

[lib]
name = "knowbase_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.5.1", features = [] }

[dependencies]
tauri = { version = "2.9.4", features = [] }
tauri-plugin-opener = "2.5.0"
